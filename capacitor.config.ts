import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'app.freesubscriptiontracker.twa',
  appName: 'SubTrack',
  webDir: 'build'
};

export default config;
